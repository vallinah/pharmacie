package main;

import java.sql.Array;
import java.util.Arrays;
import java.util.Vector;

import base.*;
import fn.All;
import fn.Function;
import gst.CRUD;

public class App {

    public static void main(String[] args) {
        try {
            // CRUD crd = new CRUD(Produit.class);
            // Vector<String> data = new Vector<>();
            // data.add("Métronidazoly");
            // data.add("4.00");
            // data.add("Manasitrana aretina fivalanana");
            // data.add("MODA00000003");
            // data.add("2025-02-22");
            //     String[] ids = {"PRO00000002", "CATP00000036"};
            // crd.update(data, ids);

            // CRUD crd = new CRUD(ProduitMaladie.class);
            // Vector<String> data = new Vector<>();
            // data.add("PRO00000005");
            // data.add("MLD00000014");
            //     String[] ids = {"PRO00000005", "MLD00000006"};
            // crd.update(data, ids);

            // CRUD crd = new CRUD(Maladie.class);
            // Vector<String> data = new Vector<>();
            // data.add("cora");
            //     String[] ids = {"MLD00000021", };
            // crd.update(data, ids);
            // System.out.println(crd.scriptUpdate());
            // crd.html_update(ids);
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}